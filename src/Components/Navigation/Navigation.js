import React from 'react';

const Navigation = () => {
	return (
		<nav style={{display: 'flex', justifyContent: 'flex-end', padding: '10 px'}}>
			<p className='f3 link dim black pa3 pointer'>Sign Out</p>
		</nav>
	);
}

export default Navigation;